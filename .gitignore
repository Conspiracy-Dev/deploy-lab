# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist
.lighthouseci
playwright-report
test-results

# Node dependencies
node_modules
.pnpm-store
.tools

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea
.serena
output

# Shared AI contour is intentionally versioned:
# .agents, .claude, .codex, .cursor, .mcp.json, .agnix.toml, AGENTS.md, CLAUDE.md

# Local env files
.env
.env.*
!.env.example

# Personal, per-developer knowledge file (see KNOWLEDGE.local.md.template).
# The template is versioned; the filled-in file is not. guard-local-files.mjs
# blocks it at commit time if it is ever staged despite this ignore rule.
*.local.md
