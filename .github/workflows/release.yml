name: select production release

on:
  workflow_dispatch:
    inputs:
      revision:
        description: Full 40-character commit SHA reachable from main
        required: true
        type: string

permissions:
  actions: read
  contents: read
  packages: read

jobs:
  release:
    uses: ./.github/workflows/production-release.yml
    with:
      release_source: manual
      revision: ${{ inputs.revision }}
